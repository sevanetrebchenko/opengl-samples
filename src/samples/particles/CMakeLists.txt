
# Configure project files.

set(SAMPLE_NAME Particle)
set(SAMPLE_SOURCE
        "${PROJECT_SOURCE_DIR}/src/samples/particles/src/main.cpp"
        )

set(SAMPLE_INCLUDE "${PROJECT_SOURCE_DIR}/src/samples/particles/include")

include(setup_project)