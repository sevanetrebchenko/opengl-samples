
# Configure project files.
set(SAMPLE_NAME Cube)

set(SAMPLE_SOURCE
        "${PROJECT_SOURCE_DIR}/src/samples/cube/src/main.cpp"
    )

set(SAMPLE_INCLUDE "${PROJECT_SOURCE_DIR}/src/samples/cube/include")

include(setup_project)